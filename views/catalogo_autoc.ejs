<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Vestidos Autóctonos - Belén Escobar</title>

    <!-- Bootstrap 4 local -->
    <link rel="stylesheet" href="/css/bootstrap.min.css" />

    <!-- Tipografía elegante -->

    <!-- Estilos personalizados -->
    <link rel="stylesheet" href="/styles.css" />
    <style>
      body {
        margin-top: 80px; /* Espacio desde el top */
      }
    </style>
  </head>
  <body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg">
      <div class="container">
        <a class="navbar-brand fw-bold text-dark" href="/">Belén Escobar</a>
        <button
          class="navbar-toggler"
          type="button"
          data-toggle="collapse"
          data-target="#menu"
          aria-controls="menu"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="menu">
          <ul class="navbar-nav ml-auto">
            <li class="nav-item">
              <a class="nav-link" href="/catalogo/nina">Niña</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/catalogo/gala">Gala</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/catalogo/15">15 años</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/catalogo/autoc">Autóctonos</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/catalogo/agenda">AGENDAR CITA</a>
            </li>
            <% if (usuario) { %>
            <li class="nav-item">
              <form action="/logout" method="POST" class="form-inline">
                <button
                  type="submit"
                  class="btn btn-sm btn-outline-danger ml-2"
                >
                  Salir
                </button>
              </form>
            </li>
            <% } else { %>
            <li class="nav-item">
              <a class="nav-link" href="/login">Login</a>
            </li>
            <% } %>
          </ul>
        </div>
      </div>
    </nav>

    <!-- Título y enlace -->
    <h1>Vestidos Autóctonos 🇧🇴</h1>
    <a href="/" class="volver">← Volver al inicio</a>

    <!-- Galería dinámica -->
    <div class="galeria">
      <% vestidos.forEach(v => { %>
      <div
        class="modal fade"
        id="modalImagen<%= v.id %>"
        tabindex="-1"
        role="dialog"
        aria-labelledby="modalImagenLabel<%= v.id %>"
        aria-hidden="true"
      >
        <div
          class="modal-dialog modal-lg modal-dialog-centered"
          role="document"
        >
          <div class="modal-content bg-transparent border-0">
            <div class="modal-header border-0">
              <button
                type="button"
                class="close text-white"
                data-dismiss="modal"
                aria-label="Cerrar"
              >
                <span aria-hidden="true">&times;</span>
              </button>
            </div>
            <div class="modal-body text-center">
              <img
                src="/imagenes/<%= v.imagen %>"
                alt="<%= v.nombre %>"
                class="img-fluid rounded"
                style="max-height: 80vh"
              />
            </div>
          </div>
        </div>
      </div>
      <div class="tarjeta">
        <img
          src="/imagenes/<%= v.imagen %>"
          alt="<%= v.nombre %>"
          data-toggle="modal"
          data-target="#modalImagen<%= v.id %>"
          class="img-fluid"
        />
        <h3><%= v.nombre %></h3>
        <p><%= v.descripcion %></p>
        <p><strong>Precio:</strong> Bs <%= v.precio %></p>
      </div>
      <% }) %>
    </div>

    <!-- Bootstrap JS para Bootstrap 4 -->
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.14.7/dist/umd/popper.min.js"></script>
    <script src="/js/bootstrap.min.js"></script>
  </body>
</html>
